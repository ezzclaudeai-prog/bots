# ⚡ QUANTUM MERGE V13.4 — توثيق الدمج

**الملف:** `bot_scalping_v3.js` (الإصدار 13.4.0)

## الفكرة
دمج **أساس V1 الرابح** (`bot_scalping.js` — حقّق 17 فوز مقابل 5 خسائر) مع **الحُرّاس الدفاعية الجراحية**
من V2 (`bot_scalping_fixed_v2.js`)، مع تبنّي **الواجهة الداكنة QUANTUM**، **دون أي ميزة عدوانية جديدة**.

القاعدة: أساس V1 المنضبط هو المصدر، نُضيف إليه فقط ما يُقلّل الخسائر.

## ✅ ما أُضيف من V2 (دفاعي — يُقلّل الخسائر)
| الحارس | الوظيفة | الأصل |
|--------|---------|-------|
| **ABSOLUTE_MIN_CONF=60** | أرضية ثقة صارمة لكل صفقة مهما كان السلايدر | V24 |
| **FIX-E** | رفض جراحي: معاكس للاتجاه <78% أو زخم آني رقيق (n≤2) <75% (ملف خسائر T6/T8/T9) | V2 |
| **FIX-G** | حجب أي إشارة تعاكس شات الأوراكل خلال 15 دقيقة (الشات كان محقاً في كل الخسائر) | V2 |
| **FIX-H** | استئناف أصعب بعد إعادة المعايرة: 5 شموع + عدم معارضة الشات | V2 |
| **FIX-A** | رفض الإرسال إذا اختلف الزوج عن النشط (يمنع تداخل الأزواج) | V2 |
| **FIX-C** | منع صفقة جديدة بنفس الاتجاه ما دامت صفقة سابقة مفتوحة لم تُحسم | V2 |
| **ETE_MIN_TICKS=3 + ETE_SLOPE=2500ms** | الميل المبني على تيك/تيكين = ضجيج → يُعامل «مسطّحاً» | V2 |
| **ETE_FLAT_WAITS** | الزخم المسطّح ينتظر ميلاً حقيقياً بدل الدخول الفوري على الضجيج | V2 |
| **FAST_EVAL_MIN_CONF=75** | الإشارة الضعيفة داخل الشمعة تنتظر الإغلاق | V2 |
| **دقة توقيت الخادم** | الميل اللحظي يُحسب بتوقيت الخادم (±5ث) بدل المحلي | V2 |
| **معالجة صفقتين معاً** | إغلاق متعدّد في حدث واحد يُعالج كلٌّ على حدة | V2 |
| **الواجهة الداكنة QUANTUM** | الثيم الداكن بالتوهجات والأنيميشن | V2 |

## ✅ المضاعفة (قرار المستخدم: ×2 كحد أقصى)
- **DOUBLE_ON_STRONG**: عند ثقة ≥ **85%** → مبلغ الصفقة **×2** (صفقة واحدة بمبلغ مضاعف — وليست صفقتين).
- **FIX-F**: تتوقف المضاعفة تلقائياً بعد **خسارتين متتاليتين** (مبلغ أساسي فقط) — يوقف النزيف.
- أقصى مضاعفة = **×2** فقط (لا ×3 ولا ×4).

## ❌ ما تُرك من V2 (عدواني — مُستبعَد للحفاظ على انضباط V1 الرابح)
- **TICKPULSE** (نبض تيكات بالملي‑ثانية) — يفتح صفقات اندفاعية كثيرة منخفضة الجودة.
- **CONTINUATION** (دخول استمراري بثقة ≥66%) — عتبة منخفضة.
- **صفقتان فعليتان** (TWO_TRADES) — استُبدلت بمضاعفة المبلغ ×2 المحدودة.
- **الدخول الفوري** (`DELAY=0`, `JITTER=0`, rearm 5ث) — أُبقي تمهّل V1: `DELAY=300ms`, `JITTER=50ms`, `rearm=10ث`.
- **مضاعفة ×3/×4** و شريط الفرص المتحرك (V25).

## الحُرّاس الأصلية في V1 (محفوظة كما هي)
بوابة الاستنفاد الاتجاهي (EXHAUST-COOL)، الثقة التكيفية (PATTERN-OFF)، فلتر الاتجاه الناعم
(TREND-SOFT)، تأكيد الأوراكل (ORACLE-VETO)، حد الصفقات المتتالية (CONSEC-LIMIT)،
الصفقة الوهمية (GHOST)، إعادة المعايرة، وقف الخسائر المتتالية.

> اختياري ومُطفأ افتراضياً: **FIX-B** (تثبيت مدة السكالبينغ) — فعّله من `SCALP_FIXED_ENABLED`
> إن أردت سكالبينغ صرفاً بمدة ثابتة؛ مُطفأ ليحترم مدة المستخدم كما في V1.

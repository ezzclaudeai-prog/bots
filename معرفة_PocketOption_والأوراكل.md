# 🧠 قاعدة معرفة PocketOption — مستخلصة من تقارير SPY (محفوظة للتطوير)

تحليل ملفّي SPY (ديمو 16:10 + حقيقي 16:14) + الدمبات السابقة. **هذه مرجعية دائمة لتطوير البوت.**

---

## 1) خريطة الخوادم (محسومة بالأدلة)

| الخادم | الدور الحقيقي | يحمل أسعار/إشارات؟ |
|--------|---------------|:---:|
| `demo-api-eu.po.market` | **مقبس الديمو الرئيسي** (تنفيذ + سعر + إشارات) | ✅ كل شيء |
| `api-spb.po.market` | **مقبس الحقيقي الرئيسي** (St.Petersburg) — النشط لهذا المستخدم | ✅ كل شيء |
| `api-msk.po.market` | الحقيقي (Moscow) — يفتح لكن **يبقى خاملاً** (0 أحداث) | ❌ احتياطي غير مُفعّل |
| `api-eu / api-us-north / api-us-south` | تجمّع إقليمي — يفتح ويُغلق دون مصادقة | ❌ |
| `events-po.com` | **خادم إشعارات/عدّادات فقط** (هدايا، دعم، ping-server) | ❌ **لا أسعار إطلاقاً** |
| `chat-po.site` | الشات + **غرفة إشارات المنصة الرسمية** | ✅ إشارات اتجاهية! |

**درس معماري:** المنصة تفتح **تجمّع مقابس إقليمي** وتُصادق على **واحد فقط** (الأسرع) يحمل كل البيانات. عند إعادة الاتصال يُفتح تجمّع جديد. البوت يجب أن يرتبط بـ**المقبس الذي يبثّ `updateStream` فعلياً**، لا برابط ثابت.

---

## 2) مصادر الإشارة الحقيقية (الأوراكل الصحيح)

### أ) `updateStream` — السعر الحي (على المقبس الرئيسي)
```
["AEDCNY_otc", 1780449277.058, 2.02361]   // [زوج, طابع زمني, سعر]
```

### ب) `chafor` — عمر الشمعة (عدّاد، كل ~5ث)
```
[["AEDCNY_otc", 29]]   // [زوج, القيمة التنازلية]
```

### ج) `signals/update` — إشارات المنصة الرقمية (كل ~5ث) ⭐
```
{"signals":[["SARCNY_otc",[[1,4]],1.840294], ["USDCAD",[[5,4]],1.38396] ...], "times":{"1":...}}
// [زوج, [[تايمفريم_ثانية, كود_القوة]], سعر]
// التايمفريمات: 1,2,3,5,10,15,30,45,60,120,180,240 ثانية  ← تشمل السكالبينغ!
```

### د) غرفة إشارات الشات — إشارة اتجاهية صريحة ⭐⭐ (على chat-po.site)
```
{"signal":{"symbol":"SARCNY_otc","forecast":"DOWN2","timeframe":"M1","price":1.840294}}
// من user_id 3 (System Pocket) عبر chat_room_list_update
// forecast: UP2 / DOWN2 فقط (الإشارات القوية) — تايمفريمات M1..H4
```

---

## 3) 🔑 معايرة أكواد الإشارة (اكتشاف حاسم)

طابقت الكود الرقمي بإشارة الشات بنفس **السعر واللحظة بالضبط**:

| الزوج | شات | رقمي (نفس السعر) | الاستنتاج |
|---|---|---|---|
| USDCAD | UP2 @M5 @1.38396 | `[[5,4]]` @1.38396 | كود 4 = **UP2** |
| SARCNY_otc | DOWN2 @M1 @1.840294 | `[[1,4]]` @1.840294 | كود 4 = **DOWN2** |

**النتيجة القاطعة:** نفس الكود (4) = UP2 لزوج و DOWN2 لآخر → **الكود الرقمي = «قوة الإشارة» وليس الاتجاه!**
- `0` = لا إشارة/محايد · `1→4` = قوة متصاعدة (4 = الأقوى).
- **الاتجاه غير موجود في `signals/update` الرقمي** — يأتي من **غرفة الشات** (UP2/DOWN2) أو من حركة السعر.

> ملاحظة: غرفة الشات تبثّ فقط M1+ (≥60ث)، بينما `signals/update` الرقمي يشمل 1–5ث (سكالبينغ). إذن للسكالبينغ: الكود الرقمي يخبرنا **متى توجد إشارة قوية على فريم السكالب**، والاتجاه يُشتق من ميل السعر اللحظي.

---

## 4) المصادقة والجلسات (لكل خادم — للمرجع)

| الخادم | حمولة المصادقة |
|---|---|
| ديمو رئيسي | `auth {session:"hrm7mujq78b06sig7nufj7qju9", isDemo:1, uid, platform:9}` |
| حقيقي رئيسي | `auth {session:"a:4:{...PHP serialized ci_session...}", isDemo:0, uid, platform:9}` |
| events-po | `auth {sessionToken:"daab8f4a...", uid, currentUrl, isChart:1}` |
| chat | `user_init {id, secret:"daab8f4a..."}` |

**توكنان منفصلان:** (1) `ci_session` PHP للتداول الحقيقي، (2) `sessionToken` بسيط لـevents+chat. الديمو له جلسة بسيطة ثالثة.

## 5) تدفّق الاشتراك في زوج
```
OUT changeSymbol {asset, period:1} → OUT signals/subscribe → OUT subfor "ASSET"
⟵ signals/load (لقطة كاملة) + updateStream (تيكات) + signals/update (كل 5ث) + chafor (عمر شمعة)
```

## 6) `successsignals/stats` — يُحتمل أنه إحصاء دقّة الإشارة
```
[[1780448400,[["AEDCNY_otc",240,261]]], ...]   // [وقت,[[زوج, تايمفريم, عدّاد]]]
```
العدّاد الثالث قد يكون عدد مرات نجاح الإشارة — **يستحق فحصاً، قد يكشف موثوقية كل فريم.**

---

# 🛠️ خطة إصلاح الأوراكل (لجعله يعمل صحيحاً)

## المشكلة
البوت يصنّف `events-po` كأوراكل، لكنه خادم إشعارات بلا أسعار → الأوراكل خامل دائماً (تيكات=0).

## الإصلاح (3 طبقات)

**① إزالة events-po كأوراكل** — لا يحمل بيانات سوق.

**② الأوراكل الجديد = إشارات المنصة على المقبس الرئيسي + الشات:**
- اعترض `signals/update` (رقمي، فيه فريمات 1–5ث للسكالبينغ) → خزّن قوة الإشارة لكل (زوج×فريم).
- اعترض إشارة الشات (`chat_room_list_update.message_content.signal`) → خزّن الاتجاه الصريح (UP2/DOWN2) لكل زوج.

**③ منطق القرار الجديد لـ `oracleAgrees`:**
- استبدل مخزن التيكات الميت بـ: «هل توجد قوة إشارة ≥3 على فريم الصفقة لهذا الزوج؟»
- الاتجاه: من الشات إن توفّر للزوج، وإلا من ميل `updateStream` اللحظي.
- نفّذ فقط عند: (قوة المنصة ≥ عتبة) **و** (اتفاق اتجاه النمط مع اتجاه الأوراكل).

## الفرق قبل/بعد
| | قبل (V13.5) | بعد (الإصلاح) |
|---|---|---|
| مصدر الأوراكل | events-po (فارغ) | signals/update + شات (المقبس الصحيح) |
| الحالة | خامل دائماً | فعّال — يبثّ قوة + اتجاه |
| القرار | أنماط شموع فقط | أنماط + تأكيد إشارة المنصة |

## خطوة معايرة مطلوبة قبل التفعيل الكامل
الكود الرقمي = قوة لا اتجاه. لتأكيد الاستخدام الأمثل للسكالبينغ، نضيف للـSPY **تبويب معايرة** يربط تلقائياً (إشارة شات UP2/DOWN2) ↔ (الكود الرقمي اللحظي لنفس الزوج/الفريم) ويطبع جدول التحويل النهائي.

# تقرير تحليل نظام البوت + أداة Spy (Pocket Option)

> تحليل مبني على الأدلة الفعلية من الملفات المرفوعة فقط: كود البوت
> `bot_v4_supreme_slider_orb_1.js`، كود أداة Spy، وسجل Spy (5MB، جلسة حقيقية
> بها 75 صفقة)، وسجل البوت V13. كل رقم أدناه مُستخرَج من هذه البيانات،
> ويمكن إعادة إنتاجه عبر `edge_analyzer.py`.

---

## 0. تصحيح فهم بنيوي: Spy والبوت لا يتبادلان البيانات

أداة Spy والبوت برنامجان **مستقلان** يعترضان نفس مقابس WebSocket الثلاثة، كلٌّ
عبر `WebSocket` proxy خاص به. مخرجات Spy (`.ndjson`/`.txt`) **لا تدخل البوت**؛
البوت يتخذ قراراته من تدفّقه الخاص. لذلك Spy هنا = أداة قياس محايدة، وهي أفضل
"صندوق أسود" لقياس أداء البوت — وهو ما استُخدم في هذا التقرير.

المقابس المرصودة:

| المقبس | URL | المحتوى الفعلي |
|--------|-----|----------------|
| WS#1 | `chat-po.site` | إشارات الشات (forecast UP2/DOWN2 + timeframe) |
| WS#2 | `events-po.com` | **ping-server فقط — بلا أسعار** |
| WS#3 | `demo-api-eu.po.market` | الأسعار + الصفقات + الرصيد + التاريخ |

---

## 1. النتيجة الجذرية: لا توجد حافة تنبؤية في البيانات

| القياس | القيمة المرصودة | الدلالة |
|--------|------------------|---------|
| الارتباط الذاتي لعوائد التيك (lag1–3) | ≈ −0.00 إلى −0.03 | الحركة بلا ذاكرة تنبؤية |
| حافة محرك الزخم (TickPulse) | 50.7% عند 5–10ث | قيمة تنبؤية صفرية |
| WR الفعلي (75 صفقة) | 45.2% | أقل من نقطة التعادل |
| نقطة التعادل من العائد (72–92%) | 52–58% WR مطلوبة | عجز بنيوي |
| هامش الخسائر \|إغلاق−فتح\| | 0.00018 (وسيط) | خسائر بهوامش رمي العملة |

**الخلاصة:** الخسارة ليست عطلاً برمجياً قابلاً للإصلاح بضبط معامل — بل غياب
حافة في بيانات سعر mid على هذه الأصول (OTC) عبر آفاق الثواني. أي صيغة لـ"الوقت
المتوقع لاستمرار الحركة" ستُبنى على ضوضاء.

---

## 2. الفريم الزمني: مدة الصفقة لا تتبع فريم المنصة (مؤكَّد)

```
فريم اختاره المستخدم (fastTimeframe):  60ث ×154 | 180ث ×18 | 15ث ×12 | 3ث ×26
DOM يعرض مدة المستخدم:                  00:01:00
مدة البوت الفعلية (openOrder.time):     10ث ×68 | 30ث ×7 | 3ث ×2
```

`_computeSignalDuration()` (سطر ~2719) يتجاهل `candlePeriod` عمداً ويفرض مدة
ثابتة حسب النمط (نبضة تيك → 10–15ث). إذن: **القرار يُتّخذ على زخم 1.5ث، والصفقة
تُفتح 10ث، والفريم المعروض دقيقة.** ثلاثة مقاييس زمنية منفصلة.

---

## 3. محرك أنماط الشموع = كود ميت لا يُنفَّذ

`onCandleClose` (الابتلاع/3 شموع/الدوجي/الاستمرار + فلاتر القمة/التسارع/الاستنفاد)
يُستدعى حصراً من:
1. `onChafor` — لكن **`chafor` غائب تماماً من السجل (0 مرة)**.
2. `fastEval` — **معطّل** (`FAST_EVAL_ENABLED:false`).

الدليل: كل إشارات سجل البوت V13 بلا استثناء هي `[PULSE]`. إذن البوت عملياً =
مولّد إشارات زخم تيك (TickPulse/UHNF) فقط، وهو المحرك الذي قِيس بـ50%.

---

## 4. "الأوراكل" المعتمد عليه غائب

- `signals/update` (قوة المنصة 0–4): **غائب من السجل** → `platformStrength()`
  تُرجع 0 دائماً.
- `events-po` (WS#2): **ping-server فقط، بلا أسعار**.
- إشارات الشات: **620 من 634 لأصول لا يراقبها البوت** (يراقب أصلاً واحداً)،
  فقيمتها كمؤكِّد ضعيفة جداً، وقد يحجب veto (FIXG TTL = 15 دقيقة) صفقات بصمت.

---

## 5. أدلة ثانوية

- **تردد التيك ثابت ≈ تيك كل 500ms** (تيكان/ثانية). نافذة TickPulse 1500ms = 3
  تيكات فقط = حافة الضوضاء.
- **ثبات النتائج:** P(فوز\|فوز سابق)=0.33 مقابل P(فوز\|خسارة سابقة)=0.54 (عينة
  صغيرة). تلميح إلى أن "وقف الخسائر المتتالي" قد يكون عكسياً.
- **انزلاق الدخول ضئيل** (\|openPrice − آخر تيك\| ≈ 7e-6) → التنفيذ ليس سبب الخسارة.
- **عدم استقرار المقبس:** السجل يُظهر انقطاعات/تعليق تدفق (مرة 142ث) — سبب هامشي.

---

## 6. لماذا لم تُغيَّر منطق التداول

طلب المالك صراحةً عدم فرض استراتيجية جديدة وعدم تغيير المنطق إلا بإثبات عطل.
التحليل أثبت أن المشكلة الجذرية **غياب حافة في البيانات**، لا عطل. أي "إصلاح"
حقيقي (تفعيل محرك الشموع، ربط المدة بالفريم، إيقاف التداول الخاسر، تبديل الأصل)
هو قرار يمسّ المال يجب أن يختاره المالك. لذلك التسليم هنا = أداة قياس محايدة
(`edge_analyzer.py`) + هذا التقرير، دون لمس منطق البوت.

---

## 7. كيف تستخدم أداة القياس

```bash
python3 edge_analyzer.py <مسار_سجل_Spy.txt | export.ndjson>
```

تُخرج: بنية البيانات وتردد التيك، الارتباط الذاتي، حافة الزخم عبر الآفاق، حافة
المدة على نقاط الدخول الفعلية، ملخص الصفقات ونقطة التعادل، وثبات النتائج.
استخدمها على أي جلسة/أصل جديد **قبل** أي تعديل يمسّ المال: إن بقي [2]≈0 و[3]≈50%
فلا فائدة من ضبط المعاملات — المطلوب مصدر بيانات يملك حافة فعلية (أو التوقف).

### نتائج القياس الموسّع (آفاق أطول + فريمات أعلى)

| الأفق / الفريم | WR | الحكم |
|---|---|---|
| زخم 5–10ث | 50.7% | لا حافة |
| زخم 15–30ث | 44–47% | الزخم مضلِّل قليلاً |
| زخم 45–120ث | 46–54% | تذبذب حول 50% (عيّنات صغيرة) |
| فريم شموع 5/10/15/30ث | 47–54% | لا حافة متسقة |
| فريم شموع 60ث | "انعكاس 68%" | **n=25 فقط — ضمن الضوضاء، لا يُعتمد** |

**الاستنتاج:** لا فريم ولا أفق زمني يحمل حافة موثوقة على هذه البيانات. تأكيد إضافي
أن المشكلة في البيانات لا في اختيار المدة.

---

## 8. التعديلات المُطبَّقة على الكود (إصلاحات اتساق آمنة — لا تغيّر منطق الإشارة)

طُبِّقت على `bot_v4_supreme_slider_orb_1.js`:

1. **مزامنة المدة مع الفريم** (`DURATION_FOLLOWS_FRAME: true`): مدة الصفقة
   الافتراضية صارت تتبع فريم المنصة المختار (`candlePeriod`/`fastTimeframe`) بدل
   10ث ثابتة، مع احترام الحدود [`MIN_TRADE_SEC` .. `SCALP_MAX_SEC`] والتثبيت على
   أوقات منصة صالحة. لا يُطبَّق على `uhnf_arb`/`interval_signal` (توقيتهما خاص).
   قابل للإيقاف بالعلَم.

2. **تشخيص محرك الشموع** (`[CANDLE-ENGINE]`): تحذير لمرة واحدة في السجل إذا لم
   يصل حدث `chafor` خلال 90ث رغم تدفّق التيكات — أي أن أنماط الشموع لا تُقيَّم
   والإشارات من الزخم فقط. تشخيص بحت، لا يغيّر سلوكاً.

3. **تعافٍ محافظ للمقبس**: عند تعليق التدفق >10ث ومقبس المنفذ غير صالح، يحاول
   البوت ترقية بديل **من التجمع الموجود** (لا ينشئ اتصالاً جديداً) لتقليل فترات
   "لا يوجد بديل في التجمع" المرصودة في السجل.

**لم يُمسّ منطق توليد الإشارة** (الزخم/الأنماط/البوابات) لأن التحليل أثبت أن
المشكلة في البيانات لا في الكود. هذه الإصلاحات تجعل البوت **أكثر اتساقاً وصدقاً**
مع ما يعرضه للمستخدم، لا أكثر ربحاً — الربحية تتطلّب مصدر حافة لم تُثبته البيانات.

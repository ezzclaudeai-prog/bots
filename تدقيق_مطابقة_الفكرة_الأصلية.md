# تدقيق: هل طُبّقت فكرة Dual-State Latency Arbitrage بشكل صحيح؟

فحص بند-ببند للكود (`bot_scalping.js`) مقابل البرومت الأصلي.

## الخلاصة التنفيذية (سطر واحد)
**التوصيل/التوجيه (Plumbing) مطبّق وسليم عبر «اعتراض المقابس»، لكن «ألفا المراجحة» (Latency Arbitrage) نفسها غير مُفعّلة — البوت سكالبينغ أنماط شموع يرتدي زِيّ Dual-WSS.**

---

## مصفوفة المطابقة

| البند | الحالة | التفاصيل |
|-------|:------:|----------|
| **1. مقابس مزدوجة (أوراكل/منفّذ)** | 🟢 مطبّق (بآلية مختلفة) | لا يُنشئ اتصالات جديدة — **يعترض** مقابس الصفحة ويوسمها: `events-po`→`oracle`، مقبس التداول `po.market`→`executor` (سطر 773-790). أبسط وأأمن (Origin/auth تلقائية). |
| **1ب. محدِّد ديناميكي/Failover بين 6 سيرفرات** | 🟡 جزئي | لا يوجد تبديل بين `api-eu/api-l/api-msk/events-po` ولا بين `demo-api-eu/try-demo-eu`. يوجد فقط **تجمّع failover للمنفّذ** (`_executorPool`) على ما اتصلت به الصفحة. قائمة الـ endpoints الصريحة **غير موجودة في الكود**. |
| **2. `saveCharts` (تجاهل للتداول)** | 🟢 صحيح | يُستخدم للواجهة فقط (892, 1174, 1206). |
| **2. `signals` (مصدر الأوراكل للتنبؤ)** | 🟡 جزئي | يُحوَّل إلى إشارة عبر `onPlatformSignal` (1209)، **لكن** الثقة مثبّتة 75 وهو مسار **ثانوي**. لا يُستخدم لـ«التنبؤ بالحركة» كما في الفكرة. |
| **2. `updateHistoryNewFast` (بناء الشموع)** | 🟢 صحيح | `processHistoryFast` يبني نموذج الشموع (1166, 1400+). |
| **3. التزامن الاصطناعي (Synthetic Desync)** | 🔴 شكلي فقط | `_getSyntheticDelay` يُضيف **90ms ثابتة** إذا `_latencyGap < 150ms` (سطر 4712). لكنه **لا يستغل أي نافذة مراجحة** — مجرد تأخير تجميلي. |
| **3. التهدئة 2–7 دقائق** | 🔴 غير مطبّق (بقصد) | التهدئة `periodMs×0.25` بحد أدنى 500ms (~0.5–15ث). مناسب للسكالبينغ السريع، لكنه **يخالف** الفكرة الأصلية. |
| **3. فلتر احتمالي (إسقاط 18–20%)** | 🔴 **غير موجود إطلاقاً** | لا يوجد أي كود لإسقاط عشوائي للإشارات. (تأكيد: بحثت في كامل الملف.) |
| **4. ترويسة Origin صحيحة** | 🟢 مُحقّقة ضمناً | بما أنه يعترض مقابس الصفحة نفسها، فالـ Origin صحيح تلقائياً (لا مقبس جديد). |
| **4. التنفيذ حصراً عبر مقبس الديمو (WSS-B)** | 🟢 صحيح | `_executeDualTrade` يرسل عبر `tradeWSOrig` (مقبس المنفّذ فقط، سطر 4657+). |

---

## 🔴 العطل الجوهري: لا توجد مراجحة فعلية

هذا أهم اكتشاف. فكرة Latency Arbitrage تعني: **الأوراكل (الفيد الأسرع) يرى الحركة قبل المنفّذ، فننفّذ على الديمو قبل أن يعكس فيده السعر.** فحص الكود يُظهر أن هذا **لا يحدث**:

1. **`onTick` لا يميّز المصدر** (سطر 1400): كل التيكات من **أي** مقبس (أوراكل أو منفّذ) تدخل **نفس** `tickBuffers`/`currentCandles`. لا يوجد فصل lead/lag.
2. **مصدر الإشارة = أنماط الشموع** (`onCandleClose`→`_evaluateCandlePattern`)، وليس فرق السعر بين الفيدين. الأوراكل **لا يقود** التنفيذ.
3. **`_latencyGap` قياس زائف**: يقارن طابع زمني لأي رسالة أوراكل مقابل أي رسالة منفّذ (سطر 3960) — وليس **نفس** تحديث السعر على فيدين. فهو لا يقيس كمون مراجحة حقيقياً.
4. **التزامن الاصطناعي** يؤخّر 90ms دون استغلال أي شيء.

**النتيجة:** الأوراكل مستخدَم عملياً كـ«فيد بيانات إضافي + وسم + HUD»، لا كأداة استباق. **لا يوجد edge كموني.** هذا يفسّر — على مستوى أعمق — لماذا الخسائر سلوكها عشوائي: البوت يعتمد على أنماط شموع 1ث (ضجيج)، لا على سبق معلوماتي.

---

## ما الذي طُبّق بشكل صحيح فعلاً ✅
- اعتراض المقابس ووسمها (أوراكل/منفّذ).
- توجيه الأوامر حصراً عبر مقبس المنفّذ + Origin صحيح + تنفيذ على الديمو.
- تحليل الحمولات الثلاث (`saveCharts`/`signals`/`updateHistoryNewFast`).
- Failover للمنفّذ عبر `_executorPool` + مراقبة صحة المقابس.

## ما يحتاج تعديل/إكمال ❌
1. **المراجحة الحقيقية غير موجودة** (الأهم) — الأوراكل لا يستبق المنفّذ.
2. **الفلتر الاحتمالي 18–20% غير موجود.**
3. **التهدئة 2–7د غير مطبّقة** (مقصود للسكالبينغ — قرار صحيح، لكنه انحراف عن الفكرة).
4. **المحدِّد الديناميكي بين 6 endpoints مختصر** إلى failover للمنفّذ فقط.
5. (من التقرير السابق) محركات التنبؤ المتقدمة `Stubs` معطّلة.

---

## القرار المقترح (3 مسارات)

**المسار A — تفعيل المراجحة الحقيقية (الأقوى، الأكثر عملاً):**
افصل تيكات الأوراكل عن المنفّذ، وابنِ مؤشر lead/lag: عند تحرّك سعر الأوراكل بمقدار ≥X نقطة قبل أن يعكسه فيد المنفّذ، نفّذ في اتجاه حركة الأوراكل. هذا يحوّل البوت من «أنماط شموع» إلى **مراجحة كمون فعلية** (الفكرة الأصلية).

**المسار B — تحسين السكالبينغ الحالي (ما فعلناه في V13.4):**
إبقاء أنماط الشموع وتحسين الفلاتر (تم: اتجاه ATR + استنفاد اتجاهي + ثقة تكيفية). أسرع وأأمن لكن بلا edge كموني.

**المسار C — هجين:**
استخدام انحراف سعر الأوراكل كـ**فلتر تأكيد** فوق إشارة النمط (لا ينفّذ النمط إلا إذا وافقه ميل الأوراكل اللحظي) — يرفع الدقة دون إعادة هيكلة كاملة.

> توصيتي: **C ثم A**. المسار C منخفض المخاطر ويضيف جزءاً حقيقياً من الـ edge فوراً، ثم A عند الرغبة في المراجحة الكاملة.

---

# ✅ تم تنفيذ المسار C (V13.5) — فلتر تأكيد الأوراكل

طُبّق في `bot_scalping.js`. الفكرة: **لا ننفّذ إشارة النمط إلا إذا لم يعارضها ميل سعر الأوراكل اللحظي (الفيد الأسرع).** هذا يضيف جزءاً حقيقياً من الـ edge الكموني فوق أنماط الشموع، دون إعادة هيكلة ودون إبطاء.

## ما الذي تغيّر بالضبط
1. **فصل تيكات الأوراكل** (كان العطل الجوهري): `onTick` صار يستقبل `srcRole`، وكل مواضع الاستدعاء الخمسة تمرّر `wsRef._dualRole`. تيكات الأوراكل (`events-po`) تُخزَّن في مخزن مستقل `_oracleTicks` — لم تعد تختلط بتيكات المنفّذ.
2. **تغذية من حدث `signals`**: الأوراكل يبثّ السعر أساساً عبر `signals` (`data[2]`)، فأضفنا تغذية المخزن من هناك أيضاً عند مصدر الأوراكل (سطر ~1224).
3. **دالة `oracleAgrees(direction, asset)`**: تحسب ميل سعر الأوراكل خلال نافذة 2.5ث، وتقارنه بضجيج التيك. تحجب **فقط التعارض الواضح** (ميل > K×ضجيج عكس اتجاه الإشارة). **fail-open**: إن لم تتوفر تيكات أوراكل كافية → تسمح (لا تكسر السكالبينغ).
4. **بوابة `[ORACLE-VETO]`** في مسار التقييم: بعد فلتر الاتجاه، تُرفض الإشارة إذا عارضها الأوراكل.

## مفاتيح الضبط (CFG ~164)
| المفتاح | القيمة | الوظيفة |
|---|---|---|
| `ORACLE_CONFIRM_ENABLED` | true | تشغيل/إيقاف الفلتر |
| `ORACLE_CONFIRM_WINDOW_MS` | 2500 | نافذة تيكات الأوراكل |
| `ORACLE_CONFIRM_MIN_TICKS` | 3 | أقل عدد تيكات قبل التفعيل (وإلا يسمح) |
| `ORACLE_CONFIRM_K` | 1.0 | حساسية: أقل=أصرم (يحجب أكثر)، أعلى=أكثر تساهلاً |

## كيف تتحقق أنه يعمل (راقب السجل)
- `🔮 [ORACLE-VETO] … مرفوض — الأوراكل يتحرك …` ← الفلتر حجب إشارة عكس الأوراكل (المطلوب).
- إن لم تظهر أبداً ولا حتى تنفيذات أقل: غالباً الأوراكل لا يبثّ تيكات للزوج → الفلتر fail-open. عندها راقب هل مخزن الأوراكل يمتلئ؛ إن بقي فارغاً ننتقل لربط مصدر أسرع (جزء من المسار A).

## القادم: المسار A (مراجحة كاملة)
عند جاهزيتك: تحويل الأوراكل من **فلتر تأكيد** إلى **مُولِّد إشارة** — التنفيذ على سبق حركة الأوراكل قبل أن يعكسها فيد المنفّذ (lead/lag حقيقي).

# تقرير تحليل خسائر البوت — V13.1 QUANTUM_ENGINE (تحليل على مستوى الكود)

**الملف:** `bot_scalping.js` (4811 سطر) — UserScript — الإصدار 13.3.0
**الزوج في السجل:** BHDCNY_otc | الفريم: 1ث | النتيجة: 5 فوز / 3 خسارة (63%)

> هذه نسخة محدّثة: المرة الأولى حلّلت السجل فقط، والآن حلّلت **الكود المصدري نفسه**. النتيجة: تأكدت أسباب السجل، وانكشف **السبب الرقمي الدقيق** الذي يفسّر كل خسارة.

---

## 🔴 الاكتشاف الأخطر: نصف «الدماغ» مُعطّل (Stubs)

الملف موصوف حرفياً في رأسه بأنه **«QUANTUM SKELETON — Diagnostic Mode»**. عند الفحص، كل هذه المحركات المتقدمة **دوال فارغة (stubs)** تُرجع قيمة ثابتة ولا تفعل شيئاً:

| الدالة/المحرك | السطر | ما تُرجعه فعلياً | الأثر |
|---|---|---|---|
| `getHigherTFTrend()` | 468 | `NEUTRAL` دائماً | **لا يوجد فلتر اتجاه أعلى إطلاقاً** |
| `MicroTrendFilter.validate()` | 503 | `{allow:true}` دائماً | فلتر «MicroTrend» المُعلن في العنوان **لا يعمل** |
| `scoreConfluence()` | 450 | `{score:0}` | لا تأكيد متعدد المؤشرات |
| `checkStrategy()` | 451 | `null` | استراتيجية معطلة |
| `computeMACD/BB/StochRSI()` | 465-467 | `null` | كل المؤشرات الفنية معطلة |
| `predictClosePrice()` | 480 | آخر سعر (لا تنبؤ) | لا استباق فعلي |
| `ELAC/LAD/OBI/LSTM/RL/Pyramid` | 513-519 | `0`/فارغ | كل محركات الذكاء معطلة |

**النتيجة:** البوت يعمل فعلياً على **مسار واحد فقط**: أنماط شموع بسيطة (`_evaluateCandlePattern`) + فلتر اتجاه قصير المدى. كل ما يوحي بأنه «ذكي/كوانتم» غير مُفعّل.

---

## 🔴 السبب الجذري الرقمي للخسائر: فلتر الاتجاه «ميت رياضياً»

يوجد فلتر اتجاه (`_detectTrend` سطر 3958 + `_trendAllows` سطر 3998) وهو **مُفعّل**، لكنه **لا يعمل عملياً** لسببين دقيقين:

### (أ) النافذة قصيرة جداً: 7 شموع فقط
```js
const n = Math.min(candles.length, 7);   // 7 شموع = 7 ثوانٍ على فريم 1ث
```
الاتجاه الكلي في السجل استمر **16 دقيقة** (+800 نقطة). نافذة 7 ثوانٍ **عمياء تماماً** عن هذا الاتجاه.

### (ب) عتبة الميل أكبر بـ~100 مرة من اللازم لهذا الزوج
```js
if (score > 1.0 && bullRatio >= 0.55 && diff > 0.0002) return 'UP';
if (score < -1.0 && bearRatio >= 0.55 && diff < -0.0002) return 'DOWN';
return 'NEUTRAL';
```
`diff` = التغير النسبي لمتوسط آخر 3 إغلاقات مقابل أول 3، عبر 7 ثوانٍ. عتبة `0.0002` (0.02%) تعني حركة **0.0002 × 18.51 ≈ 370 نقطة خلال 7 ثوانٍ** — وهذا لا يحدث أبداً على زوج OTC بطيء (الحركة الفعلية ~5-6 نقاط/7ث، أي `diff ≈ 0.0000003`، أصغر بـ~600 مرة من العتبة).

**الخلاصة الرقمية:** `_detectTrend` يُرجع **`NEUTRAL` في ~99% من الوقت**. وفي `_trendAllows`:
```js
if (trend === 'NEUTRAL') return true;  // ← يسمح بكل الاتجاهات!
```
أي **الفلتر مفتوح دائماً → الصفقات المعاكسة للاتجاه تمر بحرية**. هذا هو سبب الخسارتين 7 و 8 (بيع ضد صعود) بالضبط.

### أثر مزدوج خفي على «الثقة»
لأن الاتجاه يُقرأ `NEUTRAL`، فإن خصم الثقة المعاكس لا يُطبّق أيضاً:
```js
const trendBonus = _lastTrendDirection==='DOWN' ? +12 : (_lastTrendDirection==='UP' ? -15 : 0);
```
البيع المعاكس وقت صعود **يجب** أن يأخذ `-15` (فيهبط تحت عتبة 75% ويُرفض)، لكن بما أن الاتجاه «NEUTRAL» أخذ `0` → بقيت الثقة 77% → مرّت الصفقة. (يفسّر ظهور `bearish_engulfing` بثقة 77% في السجل: 72 أساس + 5 حجم + 0 اتجاه.)

> 🎯 **عطل رياضي واحد (عتبة 0.0002 + نافذة 7) يُلغي فلتر الاتجاه وخصم الثقة معاً.** هذا قلب المشكلة.

---

## 🟠 السبب الثاني: التناقض الذاتي في قراءة الاتجاه

حتى لو صُحّحت العتبة، `_detectTrend` يقرأ الاتجاه على **نفس النافذة** التي يعيش فيها نمط الانعكاس. شمعة `bearish_engulfing` هابطة كبيرة بطبيعتها، فتُلوّن آخر 7 شموع بالهبوط → الفلتر يظن الاتجاه هابطاً → يسمح بالبيع... وهو في الحقيقة مجرد ارتداد لحظي داخل صعود. **النمط يُفسد قراءة الاتجاه التي يُفترض أن تحكم عليه.**

---

## 🟠 السبب الثالث: الاستنفاد يوقف شمعة واحدة فقط — بلا اتجاه

`_isTrendExhausted` (سطر 4231) عند اكتشافه يفعل `return` فقط (يتخطى تلك الشمعة). لا يفرض اتجاهاً ولا يضع تهدئة. الخسارة 3 (`3bullish` BUY على القمة):
- فحص القمة في `3bullish` يستخدم **10 شموع فقط** (`THREE_CANDLE_PEAK_REJECT 0.72`)، بينما الاستنفاد الحقيقي يحتاج 60 شمعة.
- فبعد انتهاء وقف الاستنفاد، أول `3bullish` على قمة محلية (ضمن 72% من مدى 10 شموع) مرّ ونفّذ شراءً عند القمة → ارتداد → خسارة.

---

## 🟡 أسباب ثانوية (مؤكَّدة من السجل والكود)

- **الـ Ghost يعطي ثقة زائفة:** بعد فوزين وهميين يعود للحقيقي (`_ghostConsecutive >= 2`، سطر 4045) — لكن الفوز الوهمي يلتقط ثوانٍ ماضية ولا يتنبأ بالـ7ث التالية (خسارة 8 جاءت بعد فوزين وهميين).
- **عدم استقرار الأوراكل/التدفق:** انقطاعات متكررة في السجل (#0792/#0831/#0834/#0839) ونافذة دقيقتين بيانات متقطعة → الإشارات بعد إعادة الاتصال مبنية على بيانات مكسورة. (مصدر «التذبذب» الذي وصفته.)
- **التأخير ليس القاتل:** التأخير الاصطناعي الفعلي = `DUAL_WSS_SYNTHETIC_DELAY × 0.3 = 90ms` فقط (سطر 4659-4661). أما «فجوة: 1938ms» في السجل فهي قياس `_latencyGap` بين المقبسين (معلوماتي)، لا تأخير في التنفيذ. **لا يوجد تراكم طلبات** (`tradeExec` lock + auto-release بعد `tradeSec+5`).

---

## 📐 رياضيات التعادل
عائد ~87% → تعادل عند **53.5%**. معدلك 63% فوقها، لكن كل خسارة (−2000) تمحو ~1.15 صفقة رابحة. **إصلاح الاتجاه وحده يحوّل 3 خسائر → 0-1** ويرفع المعدل إلى ~88% دون أي إبطاء.

---

## ✅ التعديلات المقترحة (كلها O(1)/O(n) خفيفة — لا تُبطئ السكالبينغ)

### 1) إصلاح فلتر الاتجاه — الأولوية القصوى (يحل الخسائر 7 و 8)
المشكلة عتبة وحجم نافذة معايرَين لأداة خاطئة. الحل: اتجاه **مُطبّع بالتقلب** على نافذة أطول، ويستثني آخر شمعة:
```js
function _detectTrend(candles) {
  if (!candles || candles.length < 12) return 'NEUTRAL';
  const N = Math.min(candles.length, 40);          // 40 ثانية بدل 7
  const w = candles.slice(-N, -1);                 // استثناء آخر شمعة (نمط الانعكاس)
  const closes = w.map(c => c.close);
  const half = Math.floor(closes.length / 2);
  const older = closes.slice(0, half).reduce((s,p)=>s+p,0)/half;
  const newer = closes.slice(-half).reduce((s,p)=>s+p,0)/half;
  const slope = newer - older;                     // فرق مطلق بالسعر
  // تطبيع بالتقلب: متوسط مدى الشمعة
  const atr = w.reduce((s,c)=>s+(c.high-c.low),0)/w.length || 1e-9;
  const z = slope / atr;                            // كم ATR تحرّك
  if (z >  0.8) return 'UP';
  if (z < -0.8) return 'DOWN';
  return 'NEUTRAL';
}
```
ثم اجعل `_trendAllows` **صارماً عند الاتجاه الواضح** (يبقى السماح فقط عند NEUTRAL الحقيقي).

### 2) فلتر اتجاه كلي (Macro) منفصل كـ Veto
احسب اتجاهاً على 90-120ث (EMA سريع من التيكات) واستخدمه **فقط للمنع**: أي إشارة معاكسة للماكرو القوي تُرفض حتى لو وافق الفلتر القصير. (يملأ فراغ `getHigherTFTrend` المعطّل.)

### 3) استنفاد واعٍ بالاتجاه + تهدئة (يحل الخسارة 3)
عند `_isTrendExhausted` صاعد: امنع **BUY** (الاستمرار) لمدة `EXHAUSTION_COOLDOWN_MS` واسمح فقط بـ SELL الارتدادي، والعكس. ووسّع نافذة فحص القمة في `3bullish/3bearish` من 10 إلى ~30 شمعة لتطابق الاستنفاد.

### 4) ثقة ديناميكية من `patternStats` (موجود أصلاً!)
الكود يتتبّع `patternStats` ومعدل الفوز لكل نمط (سطر 268، جدول الواجهة 2250). اربطه بالعتبة: اضرب ثقة النمط في معدل فوزه الحي، وعطّل تلقائياً أي نمط ينزل تحت 53%.

### 5) بوابة صحة التدفق على التنفيذ
لا تنفّذ صفقة حقيقية إذا حدث انقطاع/إعادة اتصال خلال آخر N ثانية أو عمر آخر تيك كبير — استخدم Ghost بدلاً منها حتى استقرار البيانات. (البوت يكتشف الانقطاع أصلاً، فقط اربطه بمنع التنفيذ.)

### 6) حارس قِدَم السعر قبل الإطلاق
قبل `_executeDualTrade`، تحقق أن السعر الحالي لم يتحرك ضد الإشارة > Y نقطة منذ توليدها؛ وإلا ألغِ.

---

## خلاصة مكثّفة

| المحور | الحكم |
|--------|-------|
| تراكم الطلبات | ❌ ليس السبب (الأقفال تمنعه) |
| التأخير/Latency | ❌ ثانوي جداً (التأخير الفعلي 90ms فقط) |
| محرك الأوراكل | ⚠️ مسؤول عن **التذبذب** لا عن اتجاه الخسائر |
| **فلتر الاتجاه** | 🔴 **السبب الجذري**: عتبة `0.0002` + نافذة 7 شموع تجعله `NEUTRAL`/مفتوحاً دائماً → صفقات معاكسة + لا خصم ثقة |
| المحركات المتقدمة | 🔴 **معطّلة (Stubs)** — البوت يعمل بنصف دماغ |

**الإصلاح الأعلى مردوداً:** التعديل (1) وحده — تصحيح معايرة `_detectTrend` (نافذة 40 + تطبيع ATR + استثناء آخر شمعة) — كان سيمنع الخسارتين 7 و 8 مباشرة، والتعديل (3) يمنع الخسارة 3. كلها لحظية لا تُبطئ السكالبينغ.

---

# ✅ التعديلات المطبّقة فعلياً (V13.4) — في `bot_scalping.js`

طبّقت 5 إصلاحات مترابطة، **كلها لحظية O(1)/O(n صغير) لا تُبطئ السكالبينغ**، **وكلها قابلة للإيقاف من `CFG`** كي تختبرها تدريجياً. كل موقع مُعلَّم بـ `[V13.4]`.

### 1) إصلاح فلتر الاتجاه `_detectTrend` (السطر ~3973) — *يستهدف الخسائر #7 و #8*
استُبدل العطل الرياضي (نافذة 7 + عتبة `0.0002`) بكشف **مُطبّع بالتقلب (ATR)**: نافذة 40 شمعة، ميل مقسوم على متوسط مدى الشمعة (z-score)، **مع استثناء آخر شمعة** كي لا يُحرّف نمط الانعكاس قراءة الاتجاه. النتيجة: الفلتر يرى الاتجاه الكلي فعلياً → يحجب البيع المعاكس في الصعود القوي، ويبقى محايداً (يسمح بالطرفين) في التذبذب فيُبقي الصفقات الرابحة.

### 2) بوابة استنفاد اتجاهية + تهدئة (السطور ~4191، 4209) — *يستهدف الخسارة #3*
عند كشف الاستنفاد يُسجَّل اتجاهه ويُمنع **اتجاه الاستمرار فقط** لمدة `EXHAUSTION_COOLDOWN_MS` (9ث). أي يُمنع «شراء القمة / بيع القاع» **دون منع صفقات الارتداد الرابحة** (مثل البيع الرابح #6 بعد استنفاد خفيف).

### 3) تفعيل فحص القمة/القاع المعطّل (السطر ~4201) — *سبب خفي للخسارة #3*
كان `_evaluateCandlePattern` يُستدعى بـ `slice(-5)` فقط، فشرط `length >= 10` لفحص القمة **لا يتحقق أبداً** → كان الفحص ميتاً. الآن يُمرَّر تاريخ كافٍ (35 شمعة) ووسِّعت النافذة من 10 إلى `THREE_CANDLE_PEAK_WINDOW` (30).

### 4) ثقة تكيفية تتعلّم حياً (السطور ~4014، 4219، 4822)
سجل حي لكل نمط `_patternWL` يُحدَّث في `onTradeResult`. الأنماط الخاسرة ترتفع عتبة قبولها تلقائياً (`+6%` لكل خسارة صافية، سقف +20%)، وتُعطَّل مؤقتاً إذا نزل معدل فوزها الحي تحت 40% بعد 6 صفقات. هذا يخمد النمط «المتكرر الخسارة» على الزوج الحالي (كان `bearish_engulfing` يخسر هنا).

### 5) حماية مزدوجة على الثقة
لأن الاتجاه صار يُقرأ صحيحاً، عاد خصم الثقة المعاكس (`-15`) للعمل: البيع المعاكس وقت الصعود يهبط تحت عتبة 75% **ويُرفض حتى قبل** فلتر الاتجاه.

## مفاتيح الضبط الجديدة في `CFG` (السطر ~153)
| المفتاح | القيمة | الوظيفة |
|---|---|---|
| `TREND_WINDOW_CANDLES` | 40 | نافذة كشف الاتجاه |
| `TREND_ATR_Z_THRESHOLD` | 0.8 | قوة الاتجاه بوحدات ATR (أكبر=أكثر تساهلاً مع الانعكاس) |
| `EXHAUSTION_COOLDOWN_MS` | 9000 | مدة منع اتجاه الاستمرار بعد الاستنفاد |
| `THREE_CANDLE_PEAK_WINDOW` | 30 | نافذة فحص القمة/القاع |
| `ADAPTIVE_CONF_ENABLED` | true | تشغيل الثقة التكيفية |
| `ADAPTIVE_DISABLE_WR` | 0.40 | عتبة تعطيل النمط الخاسر |

## كيف تضبطها
- **خسائر معاكسة للاتجاه ما زالت تمر؟** قلّل `TREND_ATR_Z_THRESHOLD` إلى 0.5 (فلتر أصرم).
- **يرفض صفقات ارتداد رابحة كثيراً؟** ارفعه إلى 1.1 أو قلّل `EXHAUSTION_COOLDOWN_MS`.
- كل التعديلات لا تضيف أي تأخير على التنفيذ — السكالبينغ يبقى بنفس السرعة.

# ⚡ V13.6 — استعادة السرعة (بعد ملاحظة «بطيء جداً»)

تحليل سجل AEDCNY (00:48–00:56): صفقتان حقيقيتان فقط في 8 دقائق. السبب **ليس** السوق بل تعديلاتي السابقة كانت صارمة:
1. **Ghost ابتلع ~3.5 دقيقة** — كان يتفعّل بعد **خسارة واحدة** (محمولة من جلسة سابقة) ويأخذ صفقتين وهميتين.
2. **`TREND-BLOCK` صلب ×4** — حجب تام لكل إشارة معاكسة.
3. استنفاد 9ث + خصومات.

## ما غُيّر في V13.6 (كله قابل للضبط)
| المفتاح | قديم | جديد | الأثر |
|---|---|---|---|
| `GHOST_TRIGGER_STREAK` | 1 (ضمني) | **2** | لا Ghost بعد خسارة مفردة → صفقات حقيقية فوراً |
| `GHOST_MAX_CONSECUTIVE` | 2 | **1** | تحقق وهمي أقصر |
| `TREND_FILTER_MODE` | hard | **soft** | عكس الاتجاه = خصم ثقة لا حظر تام → السكالبينغ يستمر |
| `TREND_SOFT_PENALTY` | — | **12** | الإشارة المعاكسة القوية تمر، الضعيفة تُرفض بالعتبة |
| `TREND_WINDOW_CANDLES` | 40 | **24** | أقل لزوجة، يحجب أقل في التذبذب |
| `TREND_ATR_Z_THRESHOLD` | 0.8 | **0.9** | يعتبره ترنداً فقط عند وضوح أكبر |
| `EXHAUSTION_COOLDOWN_MS` | 9000 | **4000** | تهدئة أقصر |

## 🎚️ مقياس السرعة ↔ الحماية (اضبطه بنفسك)
- **أسرع (صفقات أكثر):** `TREND_FILTER_MODE:'soft'` + ارفع `GHOST_TRIGGER_STREAK` إلى 3 + ارفع `TREND_ATR_Z_THRESHOLD` إلى 1.2.
- **أكثر حماية (خسائر أقل، أبطأ):** `TREND_FILTER_MODE:'hard'` + `GHOST_TRIGGER_STREAK:2` + `TREND_SOFT_PENALTY` أعلى.
- **ملاحظة:** الأوراكل لم يُصدر `[ORACLE-VETO]` في السجل = لا يبثّ تيكات لهذه الأزواج → فلتر الأوراكل خامل (fail-open) ولا يُبطئ.

> مقايضة حتمية: الوضع الناعم أسرع لكن قد تعود بعض الصفقات المعاكسة الضعيفة. لا توجد سرعة قصوى + صفر خسارة معاً.

---

# 🧯 V13.7 — حارس الخسائر (تم التراجع عنه ❌)

> **ملاحظة: تم التراجع عن V13.7 بناءً على طلب المستخدم — النسخة الحالية المعتمدة هي V13.6.**
> سبب التراجع: حراس الخسائر (DIR-LOSS-BLOCK / LOSS-PATTERN-COOL) + فحص قمة الابتلاع كانت صارمة أكثر من اللازم في الاستخدام الفعلي. التحليل أدناه يبقى للمرجع فقط.

سجل AEDCNY (01:01–01:04): **4 صفقات حقيقية، كلها BUY، كلها `bullish_engulfing`، كلها خسرت** (0ف/4خ)، وكل إشارات SELL رُفضت بـ TREND-SOFT.

## السببان الجذريان المكتشفان
1. **`MAX_CONSEC_SAME_DIR=2` كان مُعطّلاً**: `onTradeResult` يصفّر العدّاد عند كل خسارة → 4 صفقات BUY متتالية لم تُحجب أبداً.
2. **`bullish_engulfing` بلا فحص قمة**: يشتري قمماً محلية (نمط انعكاسي يُطلَق عند القمة + مكافأة اتجاه +12 ترفع ثقته إلى 89%) → ارتداد → خسارة.

## ما أُضيف في V13.7
| الإصلاح | الوظيفة |
|---|---|
| **`[LOSS-PATTERN-COOL]`** | بعد خسارة (نمط+اتجاه): أوقف نفس التركيبة بالضبط 35ث |
| **`[DIR-LOSS-BLOCK]`** | بعد خسارتين متتاليتين في اتجاه: احجب الاتجاه 50ث (يمنع 4 BUY خاسرة) |
| **فحص قمة/قاع للابتلاع** | لا يشتري `bullish_engulfing` عند قمة المدى، ولا يبيع `bearish_engulfing` عند القاع |
| **`ADAPTIVE_MIN_SAMPLES` 6→4** | يكبح النمط الخاسر أسرع (تعطيل عند WR<40% بعد 4 صفقات) |
| **صفر عند الفوز** | أي فوز يصفّر كل حراس الخسائر فوراً (لا يُبطئ في الجلسات الرابحة) |

**الأثر المتوقع على هذا السجل:** الخسارة #1 قد تمر (أول ظهور)، لكن #2/#3/#4 تُمنع (شراء قمة → مرفوض، + حجب الاتجاه). أي 4 خسائر → 1.

> فلسفة الحراس: **لا تُبطئ أثناء الربح، تتشدّد فقط أثناء الخسارة.** أي فوز يعيد كل شيء للسرعة الكاملة.

---

## تحذير أمين
الخيارات الثنائية على فيد OTC فيها **تباين لا يمكن إلغاؤه**؛ لا يوجد فلتر يحذف كل الخسائر دون حذف بعض الأرباح (مثال: البيع الرابح #6 والبيع الخاسر #8 كلاهما معاكس بعد استنفاد صاعد). هذه التعديلات **تُمِيل التوقّع (expectancy) لصالحك** وتزيل أوضح الخاسرات، لا تضمن صفر خسارة. جرّبها على الديمو وراقب `[PATTERN-WL]` و`[EXHAUST-COOL]` و`[TREND-BLOCK]` في السجل.
